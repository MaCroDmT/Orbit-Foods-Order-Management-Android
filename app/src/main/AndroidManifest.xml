<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <application
        android:name=".OrbitFoodsApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Orbit Foods"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrbitFoods"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- LAUNCHER: SplashActivity is the first screen -->
        <activity
            android:name=".auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Auth Screens -->
        <activity android:name=".auth.LoginActivity"
            android:exported="false"/>
        <activity android:name=".auth.RegisterActivity"
            android:exported="false"/>
        <activity android:name=".auth.ForgotPasswordActivity"
            android:exported="false"/>

        <!-- Dashboard -->
        <activity android:name=".dashboard.DashboardActivity"
            android:exported="false"/>

        <!-- Product Screens -->
        <activity android:name=".products.ProductListActivity"
            android:exported="false"/>
        <activity android:name=".products.AddProductActivity"
            android:exported="false"/>

        <!-- Order Screens -->
        <activity android:name=".orders.OrderListActivity"
            android:exported="false"/>
        <activity android:name=".orders.CreateOrderActivity"
            android:exported="false"/>
        <activity android:name=".orders.ViewOrderActivity"
            android:exported="false"/>

        <!-- FileProvider for PDF sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>

</manifest>